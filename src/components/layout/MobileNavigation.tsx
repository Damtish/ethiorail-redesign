"use client";
import {useRef,useEffect} from "react";
import {useLocale,useTranslations} from "next-intl";
import {Link} from "@/i18n/navigation";
import {desktopPrimaryGroups,isActivePath} from "@/lib/navigation";
export function MobileNavigation({pathname}:{pathname:string}){const dialog=useRef<HTMLDialogElement>(null);const opener=useRef<HTMLButtonElement>(null);const overflow=useRef("");const am=useLocale()==="am";const t=useTranslations("Navigation");const close=()=>{dialog.current?.close();document.body.style.overflow=overflow.current;opener.current?.focus();};useEffect(()=>()=>{document.body.style.overflow=overflow.current;},[]);return <div className="mobile-navigation"><button ref={opener} type="button" className="icon-control" aria-label={am?"ምናሌ ክፈት":"Open menu"} aria-haspopup="dialog" onClick={()=>{overflow.current=document.body.style.overflow;document.body.style.overflow="hidden";dialog.current?.showModal();}}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" aria-hidden="true"><path d="M4 7h16M4 12h16M4 17h16"/></svg></button><dialog ref={dialog} className="mobile-drawer" aria-labelledby="mobile-title" onCancel={e=>{e.preventDefault();close();}} onClick={e=>{if(e.target===dialog.current)close();}} onKeyDown={e=>{if(e.key!=="Tab")return;const nodes=Array.from(dialog.current?.querySelectorAll<HTMLElement>('a[href],button,summary')??[]).filter(el=>el.getClientRects().length);const first=nodes[0],last=nodes[nodes.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last?.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first?.focus();}}}><div className="drawer-content"><div className="drawer-top"><strong id="mobile-title">{am?"ምናሌ":"Explore Ethio Rail"}</strong><button type="button" className="icon-control" autoFocus aria-label={am?"ምናሌ ዝጋ":"Close menu"} onClick={close}>✕</button></div><nav aria-label={am?"ዋና ምናሌ":"Mobile navigation"}><Link href="/" onClick={close} aria-current={pathname==="/"?"page":undefined}>{t("home")}</Link>{desktopPrimaryGroups.filter(g=>g.key!=="insights").map(g=><details key={g.key}><summary className={isActivePath(pathname,"/"+g.key)?"active":""}>{t(g.labelKey)} <span aria-hidden="true">+</span></summary><div className="drawer-submenu"><Link href={"/"+g.key} onClick={close}>{t(g.labelKey)} →</Link>{g.items.filter(i=>i.href!=="/"+g.key).map(i=><Link key={i.href} href={i.href} onClick={close} aria-current={isActivePath(pathname,i.href)?"page":undefined}>{t(i.labelKey)}</Link>)}</div></details>)}{(["digitalLogistics","insights","careers","bids","contact"] as const).map((k,i)=>{const href=["/digital-logistics","/insights","/careers","/bids","/contact"][i];return <Link key={k} href={href} onClick={close} aria-current={isActivePath(pathname,href)?"page":undefined}>{t(k)}</Link>;})}</nav><div className="drawer-actions"><Link href="/digital-logistics#future-portal" className="login-link" onClick={close}>{am?"ግባ · በቅርቡ":"Login · Coming soon"}</Link><Link href="/quote" className="erl-button" onClick={close}>{t("getQuote")} ↗</Link></div></div></dialog></div>;}
